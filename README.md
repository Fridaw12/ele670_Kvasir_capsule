# ele670_Kvasir_capsule


resnet50_base.py : run to train the resnet50 model
resnet50_base_test.py : run to test on testset 
embedding_model.py: run to extract embeddings from resnet50 on validation set 
plot_tsne.py: run to plot t-SNE
